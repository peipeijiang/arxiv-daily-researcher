---
title: "IntHQ：双流表示上的任务交互式分层查询用于生成式推荐"
paper_id: "2608.09634v1"
source: "arxiv"
published: "2026-08-10T14:13:47+00:00"
score: 58.0
tags: ["paper", "recommender-systems"]
---

# IntHQ：双流表示上的任务交互式分层查询用于生成式推荐

> **英文原标题**：IntHQ: Task-Interactive Hierarchical Query on Dual-Stream Representations for Generative Recommendation

[查看原文](http://arxiv.org/abs/2608.09634v1)

## 一句话结论

> 本文提出IntHQ，一种通过双流解耦、任务交互建模和层次查询来缓解多任务生成式推荐中源、关系和层次崩溃的方法，并在离线评估和Amap生产环境中显著提升了UVCTR。

## 论文信息

- **作者**：Junjie Sun, Longfei Xu, Huimin Yan, Wei Luo, Kaikui Liu, Xiangxiang Chu
- **来源**：arxiv
- **发布时间**：2026-08-10
- **相关度评分**：58.0
- **DOI**：-

<details open>
<summary><strong>中文摘要</strong></summary>

异构数据上的多任务学习是现代推荐系统的基础，而生成式模型正成为下一代推荐系统的核心支柱。然而，将多任务学习整合进生成式范式仍 largely 未被探索。现有的多任务推荐系统，无论是在判别式还是生成式范式中，都从单一的任务无关表示中提取任务相关特征，并将任务连接到一个预定义的转化漏斗中。我们表明，这种方案本质上容易遭受三重坍缩。源坍缩，即任务特定信号被过晚注入并在共享潜在空间中被稀释。关系坍缩，即任务依赖关系要么被主干网络隐式吸收，要么被预定义漏斗静态固定。层级坍缩，即任务依赖于不同尺度的特征，并在训练阶段之间发生偏移。我们提出 IntHQ，一个多任务生成式推荐系统，包含三个组件，每个组件缓解一种坍缩。双流解耦（DSD）将任务身份早期注入计算流，并将共享上下文流与任务特定流分离，从而缓解信号稀释。任务交互建模（TIM）用显式的跨任务交互取代预定义漏斗，让每个任务以其前序任务已实现的结果为条件，并采用学习到的、输入自适应的强度。层级查询（HQ）让每个任务在不同训练阶段跨不同层收集多尺度信息。在离线评估中，IntHQ 在四种代表性任务头配置下 consistently 优于具有竞争力的编码器主干网络。在高德地图上生产部署，服务数亿用户的出行推荐，IntHQ 带来了 1.60% 的相对 UVCTR 提升。

</details>

<details>
<summary><strong>英文摘要</strong></summary>

Multi-task learning over heterogeneous data is fundamental to modern recommendation, while generative models are emerging as the backbone of next-generation recommenders. However, the integration of multi-task learning into the generative paradigm remains largely unexplored. Existing multi-task recommenders, in both discriminative and generative paradigms, extract task-relevant features from a single task-agnostic representation and wire tasks into a predefined conversion funnel. We show that this scheme is inherently prone to a threefold collapse. Source collapse, where task-specific signals are injected late and diluted in the shared latent space. Relational collapse, where task dependencies are either implicitly absorbed by the backbone or statically fixed by predefined funnels. Hierarchical collapse, where tasks depend on features at different scales and shift across training stages. We propose IntHQ, a multi-task generative recommender with three components, each alleviating one collapse. Dual-Stream Decoupling (DSD) injects task identity into computation stream early and separates the shared context stream from the task-specific stream, alleviating signal dilution. Task-Interactive Modeling (TIM) replaces the predefined funnel with explicit cross-task interaction, letting each task condition on the realized outcomes of its predecessors with learned, input-adaptive strength. Hierarchical Querying (HQ) lets each task gather multi-scale information across different layers at different training stages. In offline evaluations, IntHQ consistently outperforms competitive encoder backbones under four representative task-head configurations. Deployed in production on Amap, serving hundreds of millions of users for travel recommendation, IntHQ yields a 1.60\% relative UVCTR lift.

</details>

## 深度解读

> 分析依据：**全文深读**

### 核心结论

本文针对多任务学习与生成式推荐结合时存在的三个问题：源坍缩、关系坍缩和层次坍缩，提出了IntHQ模型。IntHQ包含三个组件：双流解耦（DSD）将任务令牌与上下文令牌分离，早期注入任务信号；任务交互建模（TIM）通过自注意力实现任务间的显式自适应交互；层次查询（HQ）让每个任务从不同层聚合多尺度信息。在工业数据集IntTravel上，IntHQ在四种任务头配置下均优于基线，并在高德地图生产环境中部署，取得了1.60%的UVCTR相对提升。

### 主要创新

- 提出多任务生成式推荐中的三种坍缩问题（源坍缩、关系坍缩、层次坍缩），并给出理论分解。
- 提出双流解耦（DSD）模块，将任务令牌与上下文令牌分离，早期注入任务信号，缓解源坍缩。
- 提出任务交互建模（TIM）模块，通过自注意力实现任务间的显式自适应交互，缓解关系坍缩。
- 提出层次查询（HQ）模块，让每个任务从不同层聚合多尺度信息，缓解层次坍缩。
- 在工业级数据集和真实生产环境中验证了方法的有效性，并实现了在线部署。

### 研究方法

IntHQ采用双流架构：上下文流和任务流。上下文流处理行为序列（场景、物品、反馈令牌），任务流包含每个会话的四个任务令牌（when, where, how, via）。每层中，上下文流进行自注意力，任务流先与上下文流进行交叉注意力，再进行任务自注意力。任务流通过残差连接更新，并收集每层输出用于层次查询。层次查询使用深度注意力机制，根据任务和上下文动态选择重要层。训练采用各任务的InfoNCE损失之和。

### 关键结果

在IntTravel数据集上，IntHQ在四种任务头（PLE、STAR、DSFNet、HoME）下均优于所有基线编码器（IntTravel、OneTrans、HGenPush），在所有指标上取得最佳或次佳。消融实验显示每个组件都必要。在线A/B测试中，IntHQ相比基线带来1.60%的UVCTR相对提升，平均推理延迟40ms。

### 技术栈

- Transformer
- 自注意力机制
- 交叉注意力
- 残差连接
- 层归一化
- InfoNCE损失
- 采样softmax
- AdamW优化器
- 深度注意力
- 因果掩码

### 方法优势

- 理论分析深入，从信息论角度揭示了多任务生成式推荐中的坍缩问题。
- 方法设计针对性强，三个组件分别对应三个坍缩问题。
- 实验规模大，在工业级数据集和真实生产环境中验证。
- 与多种任务头兼容，泛化性好。
- 在线部署效果显著，具有实际应用价值。

### 主要局限

- 论文未提供代码或详细复现细节，可复现性存疑。
- 消融实验仅报告了部分指标，未展示所有任务的完整结果。
- 在线实验仅报告了UVCTR提升，未提供其他业务指标。
- 对三个坍缩的理论分析可能依赖于特定假设，通用性有待验证。
- 未讨论模型在不同数据分布下的鲁棒性。

### 与当前研究方向的关联

该论文与生成式推荐、多任务学习、序列推荐、工业落地等关键词高度相关。论文聚焦于生成式推荐中的多任务学习问题，提出了新的模型架构，并在工业场景中部署，符合推荐系统领域的最新研究方向。

---

_知识库更新时间：2026-08-11T02:42:36.410291_
