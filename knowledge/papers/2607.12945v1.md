---
title: "RecRec：序列推荐的潜在兴趣递归推理"
paper_id: "2607.12945v1"
source: "arxiv"
published: "2026-07-14T16:28:28+00:00"
score: 38.0
tags: ["paper", "recommender-systems"]
---

# RecRec：序列推荐的潜在兴趣递归推理

> **英文原标题**：RecRec: Latent Interests Recursive Reasoning for Sequential Recommendation

[查看原文](http://arxiv.org/abs/2607.12945v1)

## 一句话结论

> 针对序列推荐中推理与预测耦合导致状态瓶颈的问题，提出解耦的递归推理框架RecRec，通过上下文压缩器和递归推理器在潜在兴趣空间进行多步推理，显著提升推荐性能。

## 论文信息

- **作者**：Wenhao Deng, Junchen Fu, Hanwen Du, Alexandros Karatzoglou, Ioannis Arapakis, Hangjun Guo, Kaiwen Zheng, Yongxin Ni, Joemon M. Jose
- **来源**：arxiv
- **发布时间**：2026-07-14
- **相关度评分**：38.0
- **DOI**：-

<details open>
<summary><strong>中文摘要</strong></summary>

序列推荐系统依赖单次前向传播来编码用户交互历史并预测下一个项目。近年来，序列推荐领域开始探索通过潜在推理（latent reasoning）增加推理时的计算量，即模型在最终预测前逐步推进，并取得了有前景的结果。然而，如何为序列推荐构建推理过程仍是一个开放问题。现有方法将推理与预测耦合在单一的$d$维状态中，限制了推理深度，且通常依赖基于强化学习（reinforcement learning, RL）的多阶段流水线。我们提出RecRec（Recursive Reasoning for Recommendation，递归推理推荐），一种无强化学习的框架，将推理与预测解耦，克服了先前方法中固定的$d$维状态瓶颈。RecRec由上下文压缩器（Context Compressor）和递归推理器（Recursive Reasoner）组成，通过两个简单的监督阶段进行训练。上下文压缩器将骨干网络的隐藏状态蒸馏为一小组潜在兴趣，并引入兴趣多样性正则化器（Interest Diversity Regularizer），鼓励每个兴趣捕捉用户行为的不同方面。递归推理器随后在独立的中间潜在空间中通过推理来优化这些兴趣。深度监督使得推理深度可在推理时自由调整，无需重新训练。在四个真实世界数据集上，RecRec超越了最先进的推理增强方法；在四个数据集中的三个上，其性能提升扩展至训练时深度之外。我们的研究结果提出了一种解耦的多向量方案，将潜在推理从先前方法的单状态瓶颈中解放出来，表明推理状态结构可作为序列推荐中值得进一步探索的设计维度。

</details>

<details>
<summary><strong>英文摘要</strong></summary>

Sequential recommender systems rely on a single forward pass to encode user interaction histories and predict the next item. Increasing inference-time computation through latent reasoning, with the model proceeding step by step before the final prediction, has been recently explored in sequential recommendation with promising results. However, how to structure the reasoning process for sequential recommendation remains an open question. Existing approaches couple reasoning and prediction in a single $d$-dimensional state, limiting reasoning depth and often relying on multi-stage pipelines with reinforcement learning (RL). We propose RecRec (Recursive Reasoning for Recommendation), an RL-free framework that decouples reasoning from prediction, overcoming the fixed $d$-dimensional state bottleneck of prior methods. RecRec consists of a Context Compressor and a Recursive Reasoner, trained in two simple supervised stages. The Context Compressor distills the backbone's hidden states into a small set of latent interests, with an Interest Diversity Regularizer encouraging each interest to capture a distinct aspect of user behavior. The Recursive Reasoner then refines these interests by reasoning in a separate intermediate latent space. Deep supervision lets the reasoning depth be freely adjusted at inference without retraining. On four real-world datasets, RecRec outperforms state-of-the-art reasoning-enhanced methods, and on three of four datasets, gains extend past the training-time depth. Our findings point to a decoupled, multi-vector recipe that unleashes latent reasoning from the single-state bottleneck of prior methods, suggesting reasoning-state structure as a design axis to explore further in sequential recommendation.

</details>

## 深度解读

> 分析依据：**摘要分析**

### 核心结论

本文提出RecRec框架，用于序列推荐中的潜在推理。现有方法将推理与预测耦合在单一d维状态中，限制了推理深度，且常依赖强化学习。RecRec将推理与预测解耦，包含上下文压缩器和递归推理器，通过两个简单的监督阶段训练。上下文压缩器将骨干网络的隐藏状态蒸馏为一组潜在兴趣，并使用兴趣多样性正则化鼓励每个兴趣捕捉用户行为的不同方面。递归推理器在独立的中间潜在空间中细化这些兴趣。深度监督允许推理深度在推理时自由调整而无需重新训练。在四个真实数据集上，RecRec优于现有推理增强方法，且在三个数据集上，性能提升超过训练时的深度。研究表明，解耦的多向量方法释放了潜在推理，克服了先前方法的单状态瓶颈。

### 主要创新

- 将推理与预测解耦，克服了单一d维状态瓶颈
- 提出上下文压缩器，将隐藏状态蒸馏为多个潜在兴趣
- 引入兴趣多样性正则化，确保每个兴趣捕捉不同行为方面
- 递归推理器在独立潜在空间中细化兴趣，支持深度监督
- 推理深度可在推理时自由调整，无需重新训练

### 研究方法

RecRec包含两个模块：上下文压缩器和递归推理器。上下文压缩器将序列编码器的隐藏状态压缩为少量潜在兴趣向量，并通过兴趣多样性正则化促进兴趣分离。递归推理器在潜在空间中对这些兴趣进行多步递归细化，每一步使用深度监督信号。训练分为两个监督阶段，无需强化学习。

### 关键结果

在四个真实数据集上，RecRec优于最先进的推理增强方法；在三个数据集上，性能提升超过训练时的推理深度。

### 技术栈

- 摘要未提供具体算法、工具或数学方法细节。

### 方法优势

- 解耦推理与预测，突破状态维度限制
- 无需强化学习，训练简单
- 推理深度可灵活调整，适应不同计算预算
- 兴趣多样性正则化提升表示质量

### 主要局限

- 论文局限：摘要未提及。当前证据局限：仅基于摘要，缺乏具体实验细节、数据集名称、基线模型和消融研究。

### 与当前研究方向的关联

与序列推荐高度相关，涉及潜在推理、递归推理、兴趣表示，属于推荐系统前沿方法创新。

---

_知识库更新时间：2026-07-15T03:52:18.768245_
